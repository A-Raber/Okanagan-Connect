
# Folder where all the backend work is done


Steps to run:
- Create VE first
- Activate VE
- pip install -r requirements.txt

// use requirements.txt instead
// pip install fastapi "uvicorn[standard]" sqlmodel 

Then in the L03_Scrum-Coke directory:
- uvicorn backend.main:app --reload

To get into fastAPI:
- 127.0.0.1:8000/docs

To see website:
- 127.0.0.1:8000
